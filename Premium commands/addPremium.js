//const {db} = require("../main");
/*const Discord = require("discord.js")
module.exports = { 
    name: 'clear',
  
   async execute(client, message, args) {
     let adminId =["756083446169731102", "742342379721326643", "756085110037676102", "720196988104081451", "727761965526679643"]

     if(!adminId.includes(message.author.id)) return;

     if(adminId.includes(message.author.id)){
         if(!args[0]) return message.channel.send("Please provide me a server's ID");

         message.channel.send('Successfully added \`${args[0]}\` as a Premium server');
         db.push(`Premium`, args[0]);
     }
   }
   } */